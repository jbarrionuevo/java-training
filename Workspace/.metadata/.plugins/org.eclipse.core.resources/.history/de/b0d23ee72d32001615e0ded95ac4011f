package interfaces;

import java.util.Date;

import classes.Order;
import classes.Receipt;

public interface StoreSellerInterface {
	default Receipt sellCase(Order order){
		return new Receipt("Store name", order.getQuantity(), order.getOwner(),new Date(), order.getMyCase() );
	}
}
