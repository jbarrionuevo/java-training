package classes;

import java.util.Date;

import interfaces.StoreSellerInterface;

public class StoreSeller extends CaseSeller implements StoreSellerInterface{

	public void sellCase(Order order){
		new Receipt("Store name", order.getQuantity(), order.getOwner(),new Date(), order.getMyCase() );
	}
}
