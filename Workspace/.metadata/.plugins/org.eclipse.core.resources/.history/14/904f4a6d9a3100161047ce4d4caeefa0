package com.example.exceptions;

import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.ResponseStatus;

@ResponseStatus(value=HttpStatus.BAD_REQUEST, reason = "Specified direction must be 'up' or 'down'")
public class WrongDirectionSpecificationException extends RuntimeException{

}
